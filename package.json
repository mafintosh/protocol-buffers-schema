{
  "name": "protocol-buffers-schema-ts",
  "version": "4.0.0",
  "description": "No nonsense protocol buffers schema parser written in Javascript",
  "main": "lib/parse",
  "scripts": {
    "build": "[ -d lib ] && rm -r lib; [ -f tsconfig.tsbuildinfo ] && rm tsconfig.tsbuildinfo; mkdir -p lib && tsc && for i in lib/*.js; do mv \"$i\" \"lib/$(basename \"$i\" .js).mjs\"; done && tsc -m commonjs",
    "test": "node lib/test # && node --experimental-modules lib/test",
    "build-test": "npm run build && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ZaneHannanAU/protocol-buffers-schema"
  },
  "keywords": [
    "protobuf",
    "protocol",
    "buffers",
    "schema",
    "parser",
    "parse"
  ],
  "author": "zeen3",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ZaneHannanAU/protocol-buffers-schema/issues"
  },
  "homepage": "https://github.com/ZaneHannanAU/protocol-buffers-schema",
  "devDependencies": {
    "@types/node": "^11.13.5",
    "typescript": "^3.4.3"
  }
}
